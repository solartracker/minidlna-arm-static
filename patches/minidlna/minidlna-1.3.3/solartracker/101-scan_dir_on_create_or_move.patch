--- a/monitor_inotify.c	2023-05-31 04:25:59.000000000 -0400
+++ b/monitor_inotify.c	2026-01-31 22:10:24.406586949 -0500
@@ -333,6 +333,9 @@
 					DPRINTF(E_DEBUG, L_INOTIFY,  "The directory %s was %s.\n",
 						path_buf, (event->mask & IN_MOVED_TO ? "moved here" : "created"));
 					monitor_insert_directory(pollfds[0].fd, esc_name, path_buf);
+
+					/* --- NEW: Scan existing files recursively --- */
+					ScanDirectory(path_buf, NULL, ALL_MEDIA);
 				}
 				else if ( (event->mask & (IN_CLOSE_WRITE|IN_MOVED_TO|IN_CREATE)) &&
 				          (lstat(path_buf, &st) == 0) )
